# TaskVision (任务视界)

[English](README.md) | **中文**

---

**TaskVision (任务视界)** 是一款为 VS Code 打造的增强型视觉扩展，旨在彻底改变您追踪代码注释和 TODO 的方式。它结合了高效的搜索功能与令人惊艳的视觉风格——包括**霓虹辉光 (Neon Glow)** 和 **磨砂玻璃 (Frosted Glass)** 效果——助您高效管理技术债务和任务。

## ⚡ 快速上手 (Quick Start)

想要立即体验最棒的视觉效果？只需将以下配置复制到您的 `settings.json` 中：

```json
"taskvision.highlights.customHighlight": {
    "TODO": {
        "icon": "check",
        "foreground": "#42A5F5", // 蓝色
        "scheme": "neon+glass",   // 激活【霓虹+玻璃】双重效果
        "type": "text"            // 高亮标签及后续文本
    },
    "FIXME": {
        "icon": "alert",
        "foreground": "#FF5252", // 红色
        "scheme": "neon",        // 仅激活【霓虹辉光】
        "type": "tag"            // 仅标签发光
    },
    "HACK": {
        "icon": "beaker",
        "foreground": "#AB47BC", // 紫色
        "scheme": "glass",       // 仅激活【磨砂玻璃】
        "type": "line"           // 高亮整行
    }
}
```

保存后，您的代码注释将立即焕然一新！

## ✨ 核心功能

*   **极致视觉体验**:
    *   **霓虹模式 (Neon)**: 独特的发光文字效果，让关键信息在暗色主题下熠熠生辉。
    *   **磨砂玻璃 (Glass)**: 现代感的半透明背景色块，提供类似 iOS/macOS 的磨砂质感。
    *   **霓虹 + 玻璃 (Neon + Glass)**: 终极视觉组合。
        *   **新特性**: 支持 **“仅标签 (Tag Only)”** 模式。在此模式下，标签（如 `TODO`）会根据设置发光，而后续的注释文本将保留代码原有的语法高亮颜色，彻底解决“注释变灰”的问题，同时不影响阅读体验。
*   **全能树状视图**:
    *   自动扫描工作区中的 TODO、FIXME 等标签。
    *   支持按 **文件**、**标签** 或 **扁平列表** 分组展示。
    *   支持点击跳转，快速定位代码位置。
    *   支持导出为文件。
*   **高度可定制**:
    *   自定义任意标签（TODO, FIXME, HACK, NOTE 等）。
    *   为每个标签单独设置颜色、图标、样式和视觉方案。
    *   支持正则表达式自定义匹配规则。
*   **性能优化**:
    *   使用 Ripgrep 进行极速搜索。
    *   优化的渲染逻辑，确保即便在编辑注释时，辉光和颜色效果依然稳定存在。

## 📸 效果截图

> [!NOTE]
> *（此处等待用户补充截图：展示霓虹模式、玻璃模式及组合模式的效果）*

## 🚀 安装指南

您可以通过 Visual Studio Marketplace 安装最新版本的扩展。

或者，打开 Visual Studio Code，按下 `Ctrl+P` (Mac 上为 `Cmd+P`) 并输入：

```bash
ext install taskvision
```

## ⚙️ 配置详解

TaskVision 提供了丰富的配置项。您可以在 `settings.json` 中调整颜色、图标和行为。

### 1. 高亮设置 (Highlights)

这是扩展的核心视觉配置部分。

*   **`taskvision.highlights.defaultHighlight`**: 所有标签的默认全局设置。
*   **`taskvision.highlights.customHighlight`**: 针对特定标签的设置。

**可用属性**:
*   `foreground`: 字体颜色 (支持 Hex, RGB, 或主题颜色名)。
*   `background`: 背景颜色。
*   `icon`: 树状图中显示的图标 (支持 Octicons, e.g., "check", "alert", "beaker", "bug", "flame")。
*   `iconColour`: 单独设置图标颜色。
*   `type`: 高亮范围。
    *   `tag`: 仅高亮标签本身 (推荐用于 Neon 模式)。
    *   `text`: 高亮标签及后续文本。
    *   `line`: 高亮整行。
    *   `whole-line`: 高亮整行（铺满编辑器宽度）。
*   `scheme`: **[重要]** 视觉方案选择。
    *   `neon`: 霓虹辉光效果。
    *   `glass`: 磨砂玻璃背景效果。
    *   `neon+glass`: 组合效果。
*   `opacity`: 背景透明度 (0-100)。
*   `borderRadius`: 背景圆角大小 (例如 "4px")。
*   `gutterIcon`: 是否在行号旁显示图标 (`true`/`false`)。
*   `rulerColour`: 右侧滚动条上的标记颜色。
*   `rulerLane`: 标记在滚动条上的位置 (`left`, `center`, `right`, `full`)。

### 2. 通用设置 (General)

*   **`taskvision.general.tags`**: 定义需要搜索的标签列表。
    *   默认: `["TODO", "FIXME", "BUG"]`
*   **`taskvision.general.tagGroups`**: 标签分组设置。可以将多个标签（如 `FIXME`, `FIXIT`）归类为同一个组展示。
*   **`taskvision.general.debug`**: 是否在输出面板显示调试信息。
*   **`taskvision.general.statusBar`**: 状态栏显示内容 (`total`, `tags`, `top three`, `current file`, `none`)。
*   **`taskvision.general.statusBarClickBehaviour`**: 点击状态栏的行为 (`toggle highlights`, `reveal`, `cycle`)。

### 3. 树状视图设置 (Tree)

*   **`taskvision.tree.scanMode`**: 扫描模式。
    *   `workspace`: 扫描整个工作区（默认）。
    *   `open files`: 仅根据当前打开的文件生成树。
    *   `current file`: 仅显示当前文件。
*   **`taskvision.tree.showCountsInTree`**: 在树节点旁显示计数。
*   **`taskvision.tree.labelFormat`**: 自定义标签格式 (例如 `${tag} ${after}`)。
*   **`taskvision.tree.expanded`**: 默认展开所有节点。
*   **`taskvision.tree.flat`**: 使用扁平列表视图（不按文件夹层级）。
*   **`taskvision.tree.grouped`**: 按标签分组展示。
*   **`taskvision.tree.sortTagsOnlyViewAlphabetically`**: 标签视图按字母排序。

### 4. 正则与多行支持 (Regex)

TaskVision 使用正则表达式来查找注释中的标签。

*   **`taskvision.regex.regex`**: 用于匹配 TODO 的正则表达式。
    *   默认包含对 `//`, `#`, `;`, `<!--` 等多种注释符号的支持。
    *   regex 中的 `($TAGS)` 会被自动替换为 `general.tags` 中的内容。
*   **`taskvision.regex.enableMultiLine`**: 启用多行匹配（实验性功能）。
*   **`taskvision.regex.regexCaseSensitive`**: 是否区分大小写（默认 `true`）。

### 5. 过滤与排除 (Filtering)

*   **`taskvision.filtering.includeGlobs`**: 仅包含特定路径的文件。
*   **`taskvision.filtering.excludeGlobs`**: 排除特定路径的文件 (默认为 `["**/node_modules/*/**"]`)。
*   **`taskvision.filtering.passGlobsToRipgrep`**: 是否将 Glob 传给 Ripgrep 以提高性能。
*   **`taskvision.filtering.ignoreGitSubmodules`**: 是否忽忽略 Git 子模块。

## ⌨️ 命令列表

按 `F1` 或 `Ctrl+Shift+P` 打开命令面板，输入 `TaskVision` 即可看到所有命令：

*   **TaskVision: Add Tag**: 添加一个新的搜索标签。
*   **TaskVision: Remove Tag**: 移除一个搜索标签。
*   **TaskVision: Scan Workspace**: 手动触发工作区扫描（当自动刷新未生效时使用）。
*   **TaskVision: Export Tree**: 将当前的 TODO 列表导出为文件（支持 JSON 或文本格式）。
*   **TaskVision: Switch Scope**: 切换预定义的搜索范围（Scope）。
*   **TaskVision: Go To Next/Previous**: 跳转到当前文件中的下一个/上一个 TODO。
*   **TaskVision: Show/Hide Tree View**: 切换树状视图的显示。

## ❓ 常见问题

**Q: "Neon+Glass" 模式下，注释文字为什么不是我设置的颜色？**
A: 请检查配置。在最新版本中，只要将 `type` 设置为 `tag` 或 `text`，扩展会自动确保文字颜色跟随您的 `foreground` 设置，而不会变回灰色的默认注释颜色。

**Q: 修改注释时辉光会消失吗？**
A: 不会。我们优化了缓存机制，确保在编辑过程中辉光效果依然稳定。

**Q: 为什么找不到我的 TODO？**
A: 请检查：
1. 文件是否在 `excludeGlobs` 中（如 `node_modules`）。
2. 标签是否已添加到 `taskvision.general.tags`。
3. 也可以尝试运行 `TaskVision: Scan Workspace` 手动刷新。

---
**Credits**: Based on the excellent work of [Todo Tree](https://github.com/Gruntfuggly/todo-tree).
